- defaults:
    link:
      relink: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]
- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zsh/.zshrc


